---
import PostLayout from '@layouts/PostLayout.astro'

import { queryPostsFromIssues } from '@services'

const {
  repository: {
    issues: { nodes },
  },
} = await queryPostsFromIssues({ withContent: false })
---

<PostLayout posts={nodes} />
